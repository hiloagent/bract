{
  "name": "bract",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "tsc -b packages/runtime && tsc -b packages/runner && tsc -b packages/supervisor && tsc -b packages/cli",
    "test": "node --test packages/cli/dist/cli.test.js"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
