{
  "name": "bract",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "bun run --filter './packages/*' build",
    "test": "bun test",
    "typecheck": "bun run --filter './packages/*' typecheck"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
